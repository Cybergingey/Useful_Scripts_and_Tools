How to use an XML filter in Event Viewer:
	1. Navigate to event viewer > select the category you need the events (e.g. Windows Logs > Security)
	2. Select "Filter current log.." on the right
	3. Select the XML tab in the window that opens
	4. Select "Edit query manually" at the bottom
    5. Paste in xml